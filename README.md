# Reasoning-Content-Proxy
A local proxy that completes the tool-call reasoning feedback for models like DeepSeek and Kimi, compatible with Claude Code, to prevent tool-call errors.用于补全deepseek、kimi等模型的工具调用思维回传的本地代理，适用于claudecode，防止工具调用报错。
